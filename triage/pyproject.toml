[project]
name = "triage"
version = "0.1.0"
description = "LLM-agent root-cause hypothesis generation for failed 5G Registration/PDU-session procedures"
requires-python = ">=3.12"
dependencies = [
    "dspy>=3.3.0",
    "fastembed>=0.8.0",
    "langsmith>=0.13.0",
    "pydantic>=2.13.4",
]

[project.scripts]
triage = "triage.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["triage"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# so tests can import evals.run_eval (a script, not part of the triage
# wheel) — importing it never costs Groq calls, see test_eval.py
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    # scripts/build_corpus.py (3GPP spec corpus build, one-time step)
    "python-docx>=1.1.0",
]
